version: 2

macros:
  - name: get_monthname
    description: >
      This macro receives a release_month and returns the corresponding description.
    arguments:
      - name: release_month
        type: int
        description: > 
          release_month value.
          Must be one of the accepted values, otherwise the macro will return null